import React from 'react';
import ReactDOM from 'react-dom/client';
import WorkoutNote from './WorkoutNote.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WorkoutNote />
  </React.StrictMode>
);
